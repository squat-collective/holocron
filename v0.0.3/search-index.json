[{"slug":"","title":"Introduction","href":"/","excerpt":"Holocron documentation Everything you need to run, use, extend, and reason about Holocron. Start here | If you want to... | Read | | | | | Install Holocron in one command | [deployment.md](deployment.md) — | | Run it loc","body":"Holocron documentation Everything you need to run, use, extend, and reason about Holocron. Start here | If you want to... | Read | | | | | Install Holocron in one command | [deployment.md](deployment.md) — | | Run it locally and create your first asset | [Getting started](getting started.md) | | Understand the data model | [Concepts](concepts.md) | | See the system as a whole | [Architecture overview](architecture/specs/current architecture.md) | | Look up an endpoint | [ ](openapi.json) — also served at <http://localhost:8100/docs | Use it | Topic | Doc | | | | | Search syntax (DSL, hybrid scoring) | [search.md](search.md) | | 3D galaxy map (focus, lock, keyboard nav) | [map.md](map.md) | | Outbound webhooks (HMAC, retry, auto disable) | [webhooks.md](webhooks.md) | | UI extension framework (⌘K commands) | [extensions.md](extensions.md) | | TypeScript SDK | [ ](../packages/sdk ts/README.md) | | MCP server (Claude Desktop / Code) | [ ](../packages/mcp server/README.md) | Extend it | Topic | Doc | | | | | Catalog of built in plugins | [plugins.md](plugins.md) | | Write your own plugin | [plugins.md writing a plugin](plugins.md writing a plugin) and [ ](../packages/holocron plugin sdk/README.md) | | CLI | [plugins.md the cli](plugins.md the cli) | Operate it | Topic | Doc | | | | | One click install, GHCR images, Caddy, hardening | [deployment.md](deployment.md) | | Monorepo workflow, TDD, lint, test | [development.md](development.md) | Reference [Glossary](glossary.md) — every term in one place. [ADRs](architecture/adr/) — recorded technical decisions. 001 · [Neo4j as primary storage](architecture/adr/001 neo4j as primary storage.md) 002 · [FastAPI as web framework](architecture/adr/002 fastapi as framework.md) 003 · [Reader plugin architecture](architecture/adr/003 reader plugin architecture.md) (superseded by 006) 004 · [Multi label node model](architecture/adr/004 multi label node model.md) 005 · [Hybrid vector + fulltext search](architecture/adr/005 hybrid search.md) 006 · [Plugin SDK + entry point discovery](architecture/adr/006 plugin sdk entry points.md) 007 · [Outbound webhooks (HMAC, fire and forget)](architecture/adr/007 outbound webhooks.md) 008 · [Schema projection (Container/Field nodes)](architecture/adr/008 schema projection.md) [Specs](architecture/specs/) [Current architecture](architecture/specs/current architecture.md) — the system as it stands. [MVP architecture](architecture/specs/mvp architecture.md) — historical, January 2026. [Research notes](architecture/research/) — exploration sessions and code reviews. Conventions Cross link freely. Code paths use so they're clickable in editors. Update docs in the same PR as the code they describe. ADRs are append only — supersede with a new ADR rather than rewriting history. The OpenAPI spec is the source of truth for the API surface; regenerate with ."},{"slug":"getting-started","title":"Getting started","href":"/docs/getting-started","excerpt":"Getting started Bring up the stack, create your first asset, import a CSV, run an audit, and see results in the UI — in about 10 minutes. Prerequisites Podman or Docker with support. That's it. Nothing else needs to be i","body":"Getting started Bring up the stack, create your first asset, import a CSV, run an audit, and see results in the UI — in about 10 minutes. Prerequisites Podman or Docker with support. That's it. Nothing else needs to be installed on the host. 1. Start the stack There are two paths depending on what you're doing. a) Run Holocron (one click install) Pulls pre built images from GHCR, generates a strong Neo4j password into , brings everything up, and waits until is green. This is the recommended path for users. To pin a release: . To put it on the open internet, see the [Caddy overlay](deployment.md 2 caddy overlay public facing). b) Develop on Holocron (build from source) The dev compose mounts source for live reload and includes pytest/mypy/ruff in the API container. Use this only if you're hacking on Holocron itself. Endpoints (both paths) | Service | URL | Credentials | | | | | | UI | <http://localhost:3333 | none | | API | <http://localhost:8100 | none | | Neo4j Browser | <http://localhost:7474 | / one click: in · dev: | The first start is slow — Neo4j needs ~30 s to pass its healthcheck before the API will start. / tails; / stops; (dev only) also wipes volumes. 2. Open the UI Visit <http://localhost:3333 . You'll land on the search shell with a galaxy nebula in the background. There's nothing in the catalog yet, so the search returns no hits. Click the mode toggle in the top right to switch to the galaxy map — also empty — then back. 3. Create your first asset (UI) Hit ⌘K (or Ctrl+K ) to open the command palette. Type . Pick the wizard, fill in: Name: Type: Description: Hit Enter through the steps, Enter to confirm. The asset appears in search. 4. Create your first asset (API) Same thing, via : Or via the SDK from a Bun/Node script: See [ ](../packages/sdk ts/README.md) for the full SDK surface (Active Record, dirty tracking, typed errors). 5. Import a CSV The plugin is bundled. Use the CLI: Or, in the UI: ⌘K → Import → CSV connector → upload a file → run. The plugin scans the file, infers column types, parses any comment headers as Person actors, and creates one asset with the schema projected as Container/Field nodes. The result lands so you can review before trusting it. 6. Search Back in the UI search box: Full reference: [search.md](search.md). 7. Visit the galaxy map Switch to map mode (toggle, top right). You'll see your assets and any auto created actors as floating nodes connected by edges. Click one to focus; Shift+Enter to lock; arrow keys / vim keys to navigate. Full reference: [map.md](map.md). 8. Run an audit Open for tabs covering orphan assets, lineage dead ends, undocumented assets, dangling rules, unverified entities. There's also for the governance side and for PII candidates. Full reference: [plugins.md](plugins.md). 9. Subscribe to events (webhooks) Register a webhook to receive everything that happens: The response includes a once — store it. Every dispatch is HMAC signed; verify on your end with the recipe in [webhooks.md](webhooks.md). 10. Where to next Stay in the UI and build out the catalog with wizards. Drive everything from the SDK (TypeScript) or the API (any language). Plug Holocron into Claude Desktop or Claude Code through the [MCP server](../packages/mcp server/README.md). Write a connector for a system you have but Holocron doesn't ([plugins.md](plugins.md writing a plugin)). Troubleshooting | Symptom | Fix | | | | | hangs on Neo4j | Wait — first boot is ~30 s. to confirm. | | API returns 502 / connection refused | Neo4j isn't healthy yet. Check . | | UI shows \"Failed to fetch\" | API isn't reachable from the UI container — confirm is set. | | Search returns nothing for known assets | Embedding model loads on first request (~1 s, 300 MB). Retry. | | Plugin not listed at | Restart the API — discovery happens at startup via entry points. Confirm the package is bind mounted in . |"},{"slug":"concepts","title":"Concepts","href":"/docs/concepts","excerpt":"Concepts The data model in one page. Holocron stores everything as a Neo4j graph. There are five top level entity kinds, two derived schema kinds, six relationship types, and a small set of enums. Read this once and the ","body":"Concepts The data model in one page. Holocron stores everything as a Neo4j graph. There are five top level entity kinds, two derived schema kinds, six relationship types, and a small set of enums. Read this once and the rest of the docs make sense. Entities | Kind | Label(s) | What it is | | | | | | Asset | + one of | A piece of data or something that produces/consumes data. | | Actor | + or | A human or a team. | | Rule | | A governance/quality rule (e.g. \"must have an owner\", \"PII must be masked\"). | | Event | | An immutable audit record of a mutation. | | Webhook | | An outbound subscriber (URL + events). | | Container | | A schema container — a table, sheet, section. | | Field | | A schema column / measure. | Containers and Fields aren't created by API consumers — they're materialised by the schema projection (see below) when an Asset's is updated. Multi label nodes Assets and Actors carry both a base label ( , ) and a type specific label ( , , ...). This is Neo4j's recommended pattern and lets queries scope at either level: See [ADR 004](architecture/adr/004 multi label node model.md). Common fields Every entity has: (string, unique) — assigned at creation, never reused. (string) — display label. (string, optional). (bool) — was this confirmed by a human, or is it a plugin's guess? (string, optional) — if a plugin created it. , (datetime). (JSON object) — anything that doesn't fit a typed field. (384 dim float vector) — populated by the embedding service (BAAI/bge small en v1.5). adds , , , , , , . adds , , , , , . Relationships | Type | Source | Target | Properties | Meaning | | | | | | | | | Actor | Asset | — | Who is responsible for this asset. | | | Actor or Asset | Asset | — | Who consumes this asset. | | | Asset | Asset | — | Lineage — source → target. | | | Asset or Container | Container or Field | — | Schema tree membership (Asset → Container → Field). | | | Person | Group | — | Team membership. | | | Rule | Asset | , , | A rule attached to an asset, optionally scoped to a field. | Relations are immutable: the only edits are create and delete. To change a relation, delete and recreate it. Lineage vocabulary Holocron uses only for lineage. There's no separate / / . A process that loads a mart is just an with going in (from sources) and going out (to the mart): This keeps the graph asset only and makes lineage walks trivial: . Enums | Enum | Values | | | | | | , , , | | | , , | | | , | | | , , , , , | | | , , | | (on ) | , , | | | , , | | (on events) | , , , | Schema projection When you write an Asset whose describes containers and fields, the API materialises that JSON into real graph nodes: Container/Field nodes carry a denormalised , , and slash joined so search can show \"the field of the table\". Each gets its own embedding and lives in the vector + fulltext indexes — column level search works out of the box. The projection is rebuilt on every asset write: tear down the old subtree, materialise the new one. Simple and consistent; the cost is bounded by the per asset schema size. Source: . See [ADR 008](architecture/adr/008 schema projection.md). Audit trail (Events) Every mutation logs an node. Events are queryable ( ) and they're the trigger for outbound webhooks — register a webhook for and you'll get a signed POST every time anyone updates an asset. There's no soft delete: removes the node, but the recording the deletion stays. Identity & idempotency Connectors typically derive UIDs from a deterministic hash of the source identity (file path, table FQN, ...). That makes re scans upsert rather than create duplicates. The catalog UID is kept stable across scans even when the entity's properties change. Examples (see each connector's README for the exact recipe): — — — What's NOT modelled (yet) Authentication / RBAC — the API is open in development; production deployments put a proxy in front. Soft delete — deletes are hard; the event log is the recovery path. Per relation custom properties — only carries fields;"},{"slug":"glossary","title":"Glossary","href":"/docs/glossary","excerpt":"Glossary Every Holocron specific term in one place. Catalog Asset A piece of data or something that produces/consumes data. Has a of , , , or . Carries plus a type specific label in Neo4j. Actor A person or a team. Has a","body":"Glossary Every Holocron specific term in one place. Catalog Asset A piece of data or something that produces/consumes data. Has a of , , , or . Carries plus a type specific label in Neo4j. Actor A person or a team. Has a of or . Carries + or . Container A schema container — typically a table, sheet, or section. Created automatically by [schema projection]( schema projection); not directly editable. Field A schema column or measure. Lives under a Container. Created by schema projection. Rule A governance / data quality rule. Has ( , , ) and a . Attaches to assets via the relation, optionally scoped to a . Event An immutable audit record of one mutation ( , , ...). Drives outbound webhooks. Webhook An outbound subscription: URL + event topics + HMAC secret. The API POSTs signed payloads when matching events fire. Relationships — . Who is responsible. — . Who consumes. — . Lineage edge — source flows to target. — . Schema tree membership. — . Team membership. — . Governance attachment, with and optional . Search Hybrid search The combination of vector (semantic) and fulltext (keyword) ranking, fused into one score per result. Embedding A 384 dim float vector representing an entity's . Generated by BAAI/bge small en v1.5 via fastembed (CPU). Query DSL The small grammar that lets you write , , , , instead of plain text. See [search.md](search.md). Kind The bucket a search hit belongs to: , , , , . Returned in every search hit. Schema projection The process of materialising an Asset's JSON into real and nodes so they're searchable individually. Rebuilt on every asset write. Plugins Plugin A Python package exposing + , registered via the entry point group. Discovered by the API at startup. Capability (returns ) or (returns ). The services injected into — , , , , . Lets the plugin read/write the catalog without going through HTTP. The return type for IMPORT plugins: title, counts, samples, optional dict. The return type for EXPORT plugins: filename, content type, body bytes. Connector A plugin that pulls metadata from an external source (CSV, Excel, Postgres, Power BI, ...) and pushes it into the catalog. Exporter A plugin that produces a file from the catalog (Excel workbook, Markdown zip, ...). Extension (audit) A plugin in the audit / analysis category — , , . (Not to be confused with UI extensions.) UI extension A piece of code that contributes commands to the ⌘K palette. Lives under . See [extensions.md](extensions.md). CLI The command line driver bundled with the SDK. Lists / inspects / invokes plugins on a running API. Identity & audit UID The stable identifier assigned to every entity at creation. Strings like , , , , . Connectors typically derive UIDs deterministically from source identity so re scans upsert. Boolean flag. means a human has confirmed; means a plugin guessed it. Connectors create things with and . String tag identifying the plugin (and version) that created the entity, so you can audit provenance. Operational LOD (Level of Detail) Controls how much of the graph the map endpoint returns. is overview only (systems + teams), is everything. Topic The string used in webhook subscriptions and . E.g. . Auto disable After 10 consecutive delivery failures, a webhook is automatically disabled. Re enable by ing . Embedding model warmup The first search request after API start loads the embedding model (~1 s). After that, queries are sub 100 ms."},{"slug":"search","title":"Search","href":"/docs/search","excerpt":"Search Hybrid semantic + keyword search across every entity in the catalog, with a small DSL for power users. is the only search endpoint. The UI calls it directly; the SDK doesn't wrap it yet. What it searches | Result ","body":"Search Hybrid semantic + keyword search across every entity in the catalog, with a small DSL for power users. is the only search endpoint. The UI calls it directly; the SDK doesn't wrap it yet. What it searches | Result | Comes from | | | | | | nodes | | | nodes | | | nodes | | | Schema containers (table, sheet, section) | | | Schema columns / measures | Results are interleaved by score, not grouped by kind. A perfectly named field will rank above a vaguely matching asset. How scoring works Every searchable kind has both: A vector index over a 384 dim embedding of (BAAI/bge small en v1.5, ONNX via fastembed, CPU only). A fulltext index (Lucene through Neo4j) over . For each query, the API: 1. Parses the query into the structured (filters, kinds, phrases, negations, graph filters — see DSL below). 2. Resolves any graph filters ( , , , ) into UID sets via fulltext. 3. Fans out per kind rankers: vector top N + fulltext top N. 4. Fuses scores with the probabilistic OR rule: . 5. Applies kind discounts so schema nodes don't drown out their parent assets: container × 0.85 field × 0.80 6. Penalises pure vector hits when fulltext is dominant: if any FTS hit is above 0.55 , vector only hits are scaled by 0.55 . 7. Sorts by combined score; ties break by kind order. Constants live in and have unit tests in . See [ADR 005](architecture/adr/005 hybrid search.md) for the rationale. The query DSL Bare words are semantic + fulltext. Prefixes scope the result kind. A small set of named operators add filters and graph constraints. Kind aliases | Alias | Restricts results to | | | | | | Any asset | | | Datasets | | | Reports | | | Processes | | | Systems | | | Any actor | | | People | | | Teams / groups | | | Rules | | | Schema containers | | | Schema fields | Multiple aliases stack: returns datasets and reports matching \"revenue\". Type & severity filters | Filter | Effect | | | | | | (when used with ) constrain to a specific asset type | | | Rules of severity critical (also , ) | Graph filters | Filter | Effect | | | | | | Owned by an actor matching | | | Person is a member of a team matching | | | Uses an asset matching | | | Anywhere upstream of an asset matching | | | Applies to an asset matching | Graph filter values resolve through fulltext, so is fine even if your name is . Phrases & negations | Syntax | Effect | | | | | | Literal substring (required) | | or | Exclude any result whose name/description contains | Examples | Query | Meaning | | | | | | Anything matching \"customers\" | | | Only datasets matching \"customers\" | | | All datasets, excluding archived ones | | | Fields named (any kind) — is ignored when no rules are in scope | | | Things Tom owns that feed the revenue mart | | | Rules applied to the asset | | | Anything mentioning literal \"PII\" | Forgiveness rules The parser is intentionally lax: Unknown aliases fall back to plain text ( becomes a semantic search for ). Typos in operator names silently become semantic queries — they don't error out. Empty is allowed and returns the most recently updated entities. The trade off is power user comfort vs. error feedback. If you want strict validation, use the SDK's typed helpers (when they ship for search). Response shape is always present and is one of , , , , . Performance notes Embedding model is loaded lazily on first search request (~1 s, ~300 MB resident). After that, queries are sub 100 ms typical. Vector indexes are populated synchronously on entity write — first search after a bulk import will reflect new entities immediately. Schema projection rebuilds on every asset update; large schemas ( 1k fields) make writes slower. There's a TODO to move to a batch job. Known gaps No saved searches or query templates. No relevance feedback (clicks aren't fed back into ranking). No relations fetch from search hit (would need a follow up call to ). DSL aliases are hardcoded — not extensible without a code change."},{"slug":"map","title":"3D galaxy map","href":"/docs/map","excerpt":"3D galaxy map \"Google Maps for your data.\" Every entity is a star, every relationship is an edge, and the camera does the rest. The map lives at <http://localhost:3333/?mode=map (or click the mode toggle on the home shel","body":"3D galaxy map \"Google Maps for your data.\" Every entity is a star, every relationship is an edge, and the camera does the rest. The map lives at <http://localhost:3333/?mode=map (or click the mode toggle on the home shell). The legacy route 301s into the home shell. What it shows Nodes — every asset, actor, and rule, coloured by entity type (datasets, reports, processes, systems, people, groups, rules). Edges — every relationship, coloured by type ( , , , , , ). Animated particles flow along edges in the direction of the relationship — you can read lineage at a glance. Size scales with degree (popular nodes look bigger). The graph is laid out once at API startup with NetworkX and cached in memory. Coordinates are stable across reads; the UI gets pre computed per node. Levels of detail : — overview: only systems and teams. The \"tectonic\" view. — full graph: + datasets, reports, processes, people, rules. The UI defaults to and lets you toggle. Interactions | Action | Result | | | | | Click a node | Focus — camera glides; rest of the galaxy dims. | | Shift + Enter | Lock the focused node; navigate to others while keeping it pinned. Multiple locks supported. | | Arrow keys / vim keys ( ) | Layout agnostic neighbour navigation — picks the next node in the screen direction you indicated. | | Mouse drag | Orbit. | | Mouse wheel | Zoom. | | Pinch | Trackpad zoom. | | | Open keyboard help. | | | Clear focus and locks. | Search ↔ map The home shell has both modes; switching to map keeps the current search query. Filtering search down to a kind ( , ) also filters what's visible in the map. Selecting a search hit centres the camera on that node. Implementation pointers | Concern | Where | | | | | Map data fetch | | | 3D rendering | (react force graph 3d + three.js CSS2D) | | Galaxy nebula background | (canvas spirals + parallax) | | Layout computation (server) | | | Layout endpoint | | Limitations Layout is computed once at startup. Adding many entities later doesn't re layout — they get positioned but the global topology drifts. Restart the API to re layout. At very large scale ( 50k nodes) the 3D renderer slows. The product target is \"tens of thousands of nodes\"; beyond that, plan to filter or sample before rendering. The map is a single tenant view. There's no per user filter or workspace concept."},{"slug":"webhooks","title":"Webhooks","href":"/docs/webhooks","excerpt":"Webhooks Outbound events with HMAC SHA256 signing, fire and forget dispatch, and auto disable on failure. Every mutation in Holocron logs an node. Webhooks are subscribers: register a URL plus a list of event topics and ","body":"Webhooks Outbound events with HMAC SHA256 signing, fire and forget dispatch, and auto disable on failure. Every mutation in Holocron logs an node. Webhooks are subscribers: register a URL plus a list of event topics and you'll receive a signed POST whenever a matching event fires. See [ADR 007](architecture/adr/007 outbound webhooks.md) for the design choice. Endpoints All under : | Method | Path | Purpose | | | | | | | | Register a subscriber. Returns the secret once . | | | | List subscribers (no secrets). | | | | Get one subscriber. | | | | Update , , , or . Setting clears the failure counter. | | | | Unsubscribe. | | | | Fire a synthetic test event. Returns . | Registering Use to receive every event. The response includes the exactly once: If you don't include in the request, the API generates one (32 bytes URL safe base64). You can supply your own if you have key management constraints. Event topics A topic is : | Entity | Actions | | | | | | , , | | | , , | | | , | | | , , | Subscribe to the wildcard to receive everything (including future event kinds). Payload is a per field map for events. For and , it's empty (the full entity state lives in the audit log; query if you need it). Headers Every dispatch carries: | Header | Value | | | | | | | | | | | | e.g. | | | matches in the body | | | | Verifying the signature The signature is HMAC SHA256 over the raw request body, hex encoded. Always verify against the raw body. Re serialising JSON before HMAC will produce a different digest. Delivery semantics Fire and forget. Dispatch happens in an background task; the API request that triggered the event returns as soon as the event is logged. Bounded concurrency. A process wide caps in flight deliveries to prevent runaway tasks. No persistent retry queue (v0.1). A failure increments and stores (truncated to 500 chars) but the event is not retried later. v0.2 may add a queue. Auto disable. After 10 consecutive failures the webhook is disabled. Re enable by ing (which also resets the counter). HTTP timeout. 10 seconds per delivery. A timeout counts as a failure. Receiver responsibilities. Respond quickly. Idempotency is on you — duplicates are unlikely but not guaranteed (e.g. the API restarts mid dispatch). Testing your receiver The synthetic event has and so you can route it to a test handler. Operational notes The is currently stored plaintext in Neo4j (the dispatcher needs it to sign). Treat the database as sensitive. Webhook deliveries are logged at INFO level when they succeed and at WARN when they fail. Look for in the structured logs. Pruning old nodes is your responsibility. They're append only; no TTL is enforced."},{"slug":"extensions","title":"UI extensions","href":"/docs/extensions","excerpt":"UI extension framework Every command in the ⌘K palette comes from an extension. Pages contribute commands; built ins handle creation, navigation, sharing, governance, dev tools, and plugin invocation. This is a UI only c","body":"UI extension framework Every command in the ⌘K palette comes from an extension. Pages contribute commands; built ins handle creation, navigation, sharing, governance, dev tools, and plugin invocation. This is a UI only concept — distinct from the API plugins ([plugins.md](plugins.md)). Extensions don't run on the server; they're just functions that build commands from the current page context. Anatomy The contract A registered extension's runs whenever the palette opens. Commands are flat; group them with for the palette to render section headers. Adding an extension Then import the file once from so the registration runs. Plugin adapter bridges the API plugin registry into the palette: every plugin shows up as a command ( ), grouped by capability (Import / Export). Selecting a command opens a wizard that auto renders inputs from the plugin's manifest, then s to . Conventions Extensions are pure functions — no React, no hooks, no I/O at compute time. Side effects happen inside . can be during initial hydration — always check before calling . Use to keep the palette scannable. Existing groups: , , , , , , , , , . Commands are deduplicated by ; namespace them like . Don't fetch in — read from React Query cache via instead. Why extensions, not just inline buttons? One palette to learn (⌘K), one search, one keyboard model. Commands are dynamic: can suggest \"Apply to email field\" only when an named field is on screen. Plugin authors get a UI for free — the plugin adapter exposes their plugin without the UI team writing a button. Users discover features they didn't know existed by typing what they want."},{"slug":"plugins","title":"Plugin catalog","href":"/docs/plugins","excerpt":"Plugins Connectors push, exporters pull, audits report. All of it runs through the same plugin contract — auto discovered at API startup, driven by manifest, invoked over HTTP. How plugins work 1. A plugin is a Python pa","body":"Plugins Connectors push, exporters pull, audits report. All of it runs through the same plugin contract — auto discovered at API startup, driven by manifest, invoked over HTTP. How plugins work 1. A plugin is a Python package that exposes two attributes from one module: — slug, name, capability, inputs, etc. 2. The package registers itself via the entry point in . 3. The API discovers the entry point group at startup ( ), loads each module, and exposes them at: — list of manifests. — multipart form invocation. 4. Inside , the plugin gets a carrying the API's service layer ( , , , , ) — so it can read and mutate the catalog without going through HTTP. 5. The result is either a (returned as JSON) or a (streamed with ). There is one plugin contract and it lives in the [ ](../packages/holocron plugin sdk/) package — both the API and every plugin import from there. See [ADR 006](architecture/adr/006 plugin sdk entry points.md) for the design choice; [ADR 003](architecture/adr/003 reader plugin architecture.md) records the original \"reader\" design that this supersedes. Catalog Connectors (IMPORT) Pull metadata from somewhere, push it into the catalog. | Slug | Source | UID strategy | Notes | | | | | | | [ ](../packages/connector csv) | A / / file | | Sniffs encoding, delimiter, types. Parses comment headers as Person actors. | | [ ](../packages/connector excel) | An workbook | | Sheets, ListObjects, heuristic tables, formula lineage (VLOOKUP/XLOOKUP/INDEX MATCH), cross file refs, owners from custom properties. | | [ ](../packages/connector postgres) | PostgreSQL host + schema | | One dataset per table/view with column schema. No FK lineage in v0.1. | | [ ](../packages/connector powerbi) | A file | derived from filename + table name | Parses Layout JSON, walks DAX visual queries, emits report + referenced tables + edges. | All connectors land items as so a human can review before trusting. Exporters (EXPORT) Pull the catalog, serialise it. | Slug | Output | Use it for | | | | | | [ ](../packages/exporter excel) | One with tabs: Overview, Assets, Actors, Relations, Schemas, Lineage | Offline review, share with stakeholder, round trip back through . | | [ ](../packages/exporter markdown) | Zip of Markdown pages: , , | Drop into a docs site, browse on GitHub, feed to LLMs as context. | Extensions (audit & analysis) Read the catalog, return findings or diagnostics. | Slug | Capability | Output | | | | | | [ ](../packages/audit lineage gaps) | EXPORT | with orphan assets, lineage dead ends, undocumented entities, dangling rules, unverified items. | | [ ](../packages/compliance report) | EXPORT | with rules in force, PII inventory, ownership matrix, recent verifications, coverage stats. | | [ ](../packages/pii detector) | IMPORT | flagging high/medium confidence PII fields based on name patterns. Read only in v1 — reviewers apply flags via . | Writing a plugin The SDK ships a working template. Clone it, rename the package, edit and , run the tests: A minimum viable plugin: Register it in : Mount the package into the API container ( , see how the existing packages are wired) and restart — discovery happens at startup. Inputs drives both the API's multipart parsing and the UI's auto generated form. Three input types exist: | | UI control | Reaches your as | | | | | | | text field | | | | checkbox | / (string — coerce yourself) | | | file picker | (call ) | Result types | Return | Use when | | | | | | The plugin reports something; the UI/CLI should display structured info. Pair with . | | | The plugin produced a file. The API streams it back with . Pair with . | Conventions Set and on anything you create — it's the audit trail. Use deterministic UIDs (hash of source identity) so re runs upsert instead of duplicating. Keep to a single emoji — that's what shows on the UI plugin card. Don't import from — only from . The SDK is the public boundary. mypy strict, pydantic 2.10+. Same conventions as the rest of the monorepo. The CLI is bundled with "},{"slug":"deployment","title":"Deployment","href":"/docs/deployment","excerpt":"Deployment Holocron ships as pre built images on GHCR. One command gets you a running stack — and you're done. There are three deployment shapes, in increasing order of effort and exposure: 1. One click installer — pulls","body":"Deployment Holocron ships as pre built images on GHCR. One command gets you a running stack — and you're done. There are three deployment shapes, in increasing order of effort and exposure: 1. One click installer — pulls images from GHCR, generates a password, runs compose. LAN only. 2. Caddy overlay — same as above, fronted by Caddy with optional basic auth and auto HTTPS. Internet facing. 3. Build from source — the original dev compose at the repo root. For contributors only. 1. One click install That's it. The installer: Detects or (in that order) and refuses to continue without one. Creates with and . Generates a strong (32 chars) on first run. . Polls until the API is up, then prints the URLs. Pin to a release instead of : Custom install dir: Re running the installer is safe: it won't overwrite an existing . To upgrade, change in and run . Default endpoints | Service | URL | | | | | UI | | | API | | | Neo4j browser | (user: , password: in ) | Override the host ports via , , , in . 2. Caddy overlay (public facing) The bare prod compose has no auth. To put it on the open internet, layer the Caddy overlay on top: The overlay ( ) adds a Caddy container that: Routes to the API container, everything else to the UI. Auto issues a Let's Encrypt cert when is a real hostname. Optionally basic auths every request (uncomment the block in ). Important: the base still binds host ports for the UI, API, and Neo4j. To make Caddy the only public entry point, override those to bind in your own . 3. Build from source For contributors. The repo root is the dev stack: it builds the API image locally, mounts source for live reload, and runs the UI as . Don't deploy this anywhere. See [development.md](development.md) for the developer workflow. Images Both images are multi arch ( , ): Tag forms produced by the release workflow: | Tag | Example | When to use | | | | | | | — | Bleeding edge. Fine for tinkering, never for prod. | | | | Pin to a specific release. Recommended for prod. | | | | Same as above without the prefix. | | | | Pin to an exact commit. For debugging. | Environment reference | Var | Default | Used by | Notes | | | | | | | | (required) | neo4j, api | Set by on first run. | | | | api, ui | Image tag. Pin in prod. | | | | api, ui | Override if you forked. | | | | ui | Host port. | | | | api | Host port. | | | | neo4j | Host port. | | | | neo4j | Host port. | | | | caddy | Domain for auto HTTPS, or for plain HTTP. | Common operations Health & readiness returns once both are up. Use this for readiness probes. The Neo4j container has its own healthcheck (already wired). The UI doesn't expose a dedicated endpoint; returning 200 is the proxy for healthy. Backups Neo4j data lives in the named volume. Two paths: The API and UI are stateless — redeploy at will. Production hardening checklist The one click install is \"good enough\" for an internal LAN. Before exposing it publicly: ✅ Pin to a real tag (not ). ✅ Use the Caddy overlay or your own reverse proxy. The API and UI have no auth. ✅ Set a strong (the installer does this for you). ⚠️ Pin upstream image digests too ( , ) if you need byte identical deploys. ⚠️ Hit any search endpoint after deploy to warm the embedding model into RAM (~300 MB). Pre warm at boot if first request latency matters. ⚠️ For high throughput, run multiple API workers behind the proxy. The webhook dispatcher's semaphore is per process; more workers = more concurrent dispatches. ⚠️ Set up backups for . Releases Cutting a release is one command: then: 1. Builds and for amd64 + arm64. 2. Pushes to GHCR with , , , and tags. 3. Creates a GitHub Release with , , , and attached. Users then and are running your tag."},{"slug":"development","title":"Development","href":"/docs/development","excerpt":"Development How to work on Holocron without installing anything on your host. Ground rules Containerised only. Podman first, Docker as fallback. The auto detects whichever is on . TDD. Write the failing test, then the co","body":"Development How to work on Holocron without installing anything on your host. Ground rules Containerised only. Podman first, Docker as fallback. The auto detects whichever is on . TDD. Write the failing test, then the code. Every package is configured to run tests in a container with one command. KISS. Build what's needed. No speculative abstractions. No half finished features. Strict types. mypy for Python, for TypeScript, no . One PR updates docs and code together. If you change behaviour, update the doc that describes it. Each package's has the package specific take on these rules. Monorepo layout JS workspaces ship via Bun ( at the root). Python plugins are independent packages installed editable into the API container. Daily commands The root forwards , , to each package's own Makefile, so is just . Dev image vs released image The dev compose builds with so pytest, mypy, and ruff land in the API container — that's why works. The release workflow builds the same Dockerfile without the arg, producing a slimmer (~94 MB lighter) GHCR image with runtime deps only. If you need to mirror the prod image locally: The UI image is multi stage ( ) and only used for releases — local dev runs Next as against bind mounted source. Adding a feature: full loop The \"right\" loop is the same regardless of which package you touch: 1. Write the failing test. Pytest for API, Vitest for UI, Bun test for SDK / MCP. 2. Implement until green. 3. Lint + typecheck. (Python) or / (TS). 4. Run the full suite. , , . 5. If you touched the API surface, regenerate docs. . The SDK regenerates its types from the spec via . 6. Update the relevant doc page. Don't ship behaviour changes without updating . 7. Commit. Conventional style messages ( , , , ...). The git history is the changelog. Adding an API endpoint 1. Pydantic schema in . 2. Service method in . 3. Repository method in if it touches Neo4j. 4. Route in — call the service, raise the right exceptions. 5. Test (unit for the service, integration for the route). 6. to refresh . 7. Add a method to the SDK if it's a public API. Patterns to copy: is the canonical CRUD, is the canonical \"create returns extra once\" pattern. Adding a UI feature 1. Test first (Vitest + RTL). 2. If the feature needs API data, check the SDK has a method. If not, add it to the SDK first , rebuild ( ), then consume it from the UI. 3. Server components by default; only when you need state, effects, or browser APIs. 4. Data fetching: TanStack Query in client components, direct SDK call in server components / route handlers. 5. Style with Tailwind v4 + shadcn/ui primitives. The galaxy palette is in as oklch tokens. 6. If it's a command, expose it via an [extension](extensions.md) so it shows up in ⌘K. Adding a plugin See [plugins.md writing a plugin](plugins.md writing a plugin). The TL;DR: Testing strategy | Layer | Tool | When | | | | | | Pure functions, scoring, parsing | pytest unit | Always. | | API routes | pytest integration (with Neo4j fixtures) | For every endpoint. | | SDK | Bun test (mocked fetch) | For every public method. | | UI components | Vitest + RTL (happy dom) | For every component with logic. | | UI critical paths | Playwright | For critical journeys. Don't bloat with low value flows. | Integration tests must hit a real Neo4j . The compose stack provides one; uses it. Code review checklist [ ] Tests cover the new behaviour and exercise the failure modes. [ ] No new / . No unjustified / . [ ] Public API change → SDK change → UI change, in that order. [ ] OpenAPI spec regenerated if the API surface moved. [ ] Doc updated in the same PR. [ ] No new dependencies without a one line rationale in the PR description. [ ] No commented out code, no , no debugging left behind. [ ] Commit messages explain why , not what . Where to put things | Question | Answer | | | | | New entity type | Pydantic model in , schema in , repository in , route in , ADR if it's a meaningful design choice. | | New domain service | + factory. | | N"},{"slug":"architecture/specs/current-architecture","title":"Current architecture","href":"/docs/architecture/specs/current-architecture","excerpt":"Current architecture The system as it stands. Supersedes [ ](mvp architecture.md) (January 2026). Date: 2026 04 26 Status: Living document — kept current alongside the code. Components | Component | Stack | Lives in | | ","body":"Current architecture The system as it stands. Supersedes [ ](mvp architecture.md) (January 2026). Date: 2026 04 26 Status: Living document — kept current alongside the code. Components | Component | Stack | Lives in | | | | | | API | Python 3.12 · FastAPI · Pydantic v2 · neo4j async | | | Plugin contract | Python 3.12 · pydantic | | | Built in plugins | Python 3.12 | , , , , | | TypeScript SDK | TS · Bun · openapi fetch · openapi typescript | | | UI | Next.js 15 · React 19 · TanStack Query v5 · Tailwind v4 · shadcn/ui · react force graph 3d | | | MCP server | TS · · zod | | | Storage | Neo4j 5 with APOC, vector + fulltext indexes | bundled image | Data model Single Neo4j graph. Entities: , , , , , plus derived and from schema projection. Edges: , , , , , . Multi label nodes per [ADR 004](../adr/004 multi label node model.md). Full reference in [ ](../../concepts.md). Constraints + indexes are created at startup ( ): Unique constraints on for every primary label. Vector indexes (cosine, 384 dim) on , , , , . Fulltext indexes on for the same set. Request flow (read) The UI never talks to Neo4j directly. The browser never talks to the API directly — it goes through Next.js route handlers under so the API base URL never leaks client side. Request flow (write) Same path, plus: 1. The service writes the entity through the repository. 2. The service updates the embedding (if applicable) via . 3. If the entity is an Asset with , the schema projection rebuilds the / subtree. 4. The service logs an via . 5. triggers which fans out HMAC signed POSTs in background tasks (bounded by a process wide semaphore of 100). 6. The API response returns immediately — webhooks don't block. Plugin lifecycle Per [ADR 006](../adr/006 plugin sdk entry points.md). The original MVP era reader system is documented in [ADR 003](../adr/003 reader plugin architecture.md) and superseded by this. Search Hybrid vector + fulltext, fused per kind, scored with discounts and thresholds in . DSL parsed in . Full reference in [ ](../../search.md). Design in [ADR 005](../adr/005 hybrid search.md). 3D map NetworkX runs once at API startup; coordinates are cached in process and served from . The UI renders with + three.js CSS2D labels. Full reference in [ ](../../map.md). Webhooks CRUD on nodes. HMAC SHA256 signing over the raw body. Fire and forget dispatch via background tasks. 10 consecutive failures → auto disable. No persistent retry queue in v0.1. Full reference in [ ](../../webhooks.md). Design in [ADR 007](../adr/007 outbound webhooks.md). What changed since the MVP spec Compared to [ ](mvp architecture.md): | Then (Jan 2026) | Now (Apr 2026) | | | | | API only | API + TS SDK + UI + MCP server | | returning suggestions for human approval | (entry point discovery) running in process with full service access | | , , , , , , , | , , , , , — lineage collapsed to only; added for rules | | No search | Hybrid vector + fulltext with DSL | | No UI | Next.js portal with search + 3D map + wizards + ⌘K extensions | | No webhooks | Outbound webhooks (HMAC, fire and forget, auto disable) | | route | Removed — lineage is graph traversal, surfaced via the map and the UI lineage view | | 4 entity types | 5 + 2 derived (Container/Field via schema projection) | | No rules | First class + | | No events | Audit log driving webhooks | The MVP spec is preserved as historical context; this page is the current reference. Known limitations No auth. Production deployments must put a proxy in front. See [deployment.md](../../deployment.md production notes). No soft delete. Hard deletes only. Recovery path is the event log + re import. Schema projection is full rebuild on every asset write. Bounded by per asset schema size. At scale, move to a batched/incremental rebuild. Graph layout cached at startup. Adding many entities later doesn't re layout — restart to refresh. No persistent webhook retry. A failed delivery is not retried beyond the immediate attempt; 10 failures auto disable. Webhook"},{"slug":"architecture/adr/001-neo4j-as-primary-storage","title":"ADR-001 · Neo4j as primary storage","href":"/docs/architecture/adr/001-neo4j-as-primary-storage","excerpt":"ADR 001: Neo4j as Primary Storage Date: 2026 01 27 Status: Accepted Deciders: Tom Context Holocron needs to store data assets, actors (people/groups), and their relationships. The core value proposition is visibility — u","body":"ADR 001: Neo4j as Primary Storage Date: 2026 01 27 Status: Accepted Deciders: Tom Context Holocron needs to store data assets, actors (people/groups), and their relationships. The core value proposition is visibility — understanding what data exists and how it connects. Key requirements: Store entities with flexible metadata Track typed relationships between entities Query lineage (upstream/downstream traversal) Answer \"what depends on X?\" questions efficiently Decision Use Neo4j as the primary (and only) database. Options Considered 1. PostgreSQL (Relational) Pros: Familiar, battle tested Strong ACID guarantees Good tooling ecosystem Cons: Recursive CTEs for lineage queries are complex and slow Schema migrations for new entity types JOIN heavy queries for relationship traversal 2. MongoDB (Document) Pros: Flexible schema Easy to start Good for metadata blobs Cons: No native graph traversal Would need application level relationship handling Lineage queries become complex 3. Neo4j (Graph) ✅ Selected Pros: Native graph traversal (lineage in one query) Cypher is expressive for relationship queries Multi label nodes fit our model perfectly Built in visualization (Neo4j Browser) Cons: Less familiar than SQL Smaller talent pool Overkill if we don't use graph features 4. Hybrid (Postgres + Neo4j) Pros: Best of both worlds Cons: Sync complexity Two databases to maintain Overkill for MVP Rationale The core value of Holocron is understanding relationships: \"What feeds into this report?\" \"What breaks if I change this dataset?\" \"Who owns the things that depend on X?\" These are graph traversal queries . In a relational DB, they require recursive CTEs or multiple round trips. In Neo4j, they're single Cypher queries: Given that relationship traversal is the core feature , Neo4j is the right tool. Consequences Positive Lineage and impact queries are simple and fast Data model maps naturally to graph Can visualize relationships in Neo4j Browser Cypher is readable for non engineers Negative Team needs to learn Cypher Fewer hosting options than Postgres May need Postgres later for other features (auth, audit logs) Mitigations Document common Cypher patterns Use Neo4j's official Python driver (well supported) Keep the door open for adding Postgres later if needed References [Neo4j Documentation](https://neo4j.com/docs/) [Cypher Query Language](https://neo4j.com/developer/cypher/)"},{"slug":"architecture/adr/002-fastapi-as-framework","title":"ADR-002 · FastAPI as web framework","href":"/docs/architecture/adr/002-fastapi-as-framework","excerpt":"ADR 002: FastAPI as Web Framework Date: 2026 01 27 Status: Accepted Deciders: Tom Context Holocron needs a REST API as its core interface. The API will be consumed by: Web UI (primary, future) CLI tools (future) MCP inte","body":"ADR 002: FastAPI as Web Framework Date: 2026 01 27 Status: Accepted Deciders: Tom Context Holocron needs a REST API as its core interface. The API will be consumed by: Web UI (primary, future) CLI tools (future) MCP integrations (future) Direct API calls Requirements: RESTful endpoints with JSON Request/response validation Auto generated API documentation Async support (for Neo4j driver) Type safety Decision Use FastAPI as the web framework. Options Considered 1. Flask Pros: Simple, minimal Large ecosystem Very flexible Cons: No built in validation No auto generated docs Sync by default (async is bolt on) Manual OpenAPI spec 2. Django + DRF Pros: Batteries included Strong ORM Admin interface Cons: Heavy for our use case (no SQL ORM needed) Opinionated structure Async support is recent/limited 3. FastAPI ✅ Selected Pros: Native async/await Pydantic integration (validation + serialization) Auto generated OpenAPI docs Type hints everywhere Modern Python (3.10+) Cons: Smaller ecosystem than Flask/Django Less \"magic\" (more explicit code) 4. Litestar (formerly Starlite) Pros: Similar to FastAPI Some performance improvements Cons: Smaller community Less documentation Unnecessary risk for MVP Rationale 1. Pydantic Integration : We're already using Pydantic for domain models. FastAPI's native integration means request validation, response serialization, and OpenAPI generation come free. 2. Async Native : Neo4j's Python driver supports async. FastAPI makes async endpoints natural, not bolted on. 3. API Documentation : Auto generated Swagger/OpenAPI docs help us iterate quickly and help future UI/CLI developers understand the API. 4. Type Safety : FastAPI enforces type hints, aligning with our \"strict typing\" guideline. Consequences Positive Instant API documentation at Type errors caught at development time Clean async code with Neo4j Pydantic schemas shared between API and domain Negative Team needs FastAPI knowledge Some Flask/Django patterns don't apply Dependency injection style may feel different Mitigations FastAPI docs are excellent Patterns are well established Most Python developers pick it up quickly References [FastAPI Documentation](https://fastapi.tiangolo.com/) [Pydantic v2 Documentation](https://docs.pydantic.dev/)"},{"slug":"architecture/adr/003-reader-plugin-architecture","title":"ADR-003 · Reader plugin architecture","href":"/docs/architecture/adr/003-reader-plugin-architecture","excerpt":"ADR 003: Reader Plugin Architecture Date: 2026 01 27 Status: ⚠️ Superseded by [ADR 006](006 plugin sdk entry points.md) (2026 04 26) Deciders: Tom The \"reader\" naming and the API have been replaced by the generic plugin ","body":"ADR 003: Reader Plugin Architecture Date: 2026 01 27 Status: ⚠️ Superseded by [ADR 006](006 plugin sdk entry points.md) (2026 04 26) Deciders: Tom The \"reader\" naming and the API have been replaced by the generic plugin system: a public package, entry point discovery via the group, in process invocation through , and three capability families (connectors, exporters, audit/extension plugins). The \"suggest don't write\" model was dropped — plugins now have full service access via . See [ADR 006](006 plugin sdk entry points.md) for the rationale. Context Holocron needs \"readers\" — components that scan external sources (Excel files, databases, etc.) and suggest metadata about data assets. This is how Holocron learns about existing data without requiring 100% manual entry. Key requirements: Readers should be extensible (add new source types) Readers output suggestions , not direct writes (human approval required) Simple to implement new readers Must work in containerized environment Decision Implement readers as in process Python plugins that return suggestions via API. Reader Behavior 1. Readers are triggered via API: 2. Readers receive a source path/URL and options 3. Readers return suggestions (ephemeral, not persisted) 4. Humans review suggestions and create assets via separate API calls Plugin Structure Options Considered 1. In Process Python Modules ✅ Selected Pros: Simplest to implement No IPC overhead Easy debugging Shared types with core Cons: Bad reader can crash the app All readers must be Python Memory shared with main process 2. Subprocess (exec) Pros: Process isolation Can be any language Crash isolation Cons: IPC complexity (stdin/stdout JSON) Harder to debug Dependency management per reader 3. Container per Reader Pros: Maximum isolation Independent scaling Any language/runtime Cons: Complex orchestration Slow startup Overkill for MVP 4. External Services (webhooks) Pros: Fully decoupled Can run anywhere Cons: Network complexity Auth/security concerns Deployment overhead Rationale For MVP, simplicity wins . In process plugins: Are trivial to implement (just Python classes) Share Pydantic models with the core Can be unit tested easily Don't require container orchestration The design doesn't prevent moving to subprocess or containers later. The interface is the contract — implementation can change. Suggestion Model (Not Direct Writes) Critical design choice: readers suggest , they don't create. Why: Human stays in control — readers can be wrong Auditability — humans are accountable for what's in Holocron Simplicity — readers don't need write access to DB KISS — no approval workflow state machine needed Consequences Positive Easy to write new readers (just extend BaseReader) No infrastructure complexity Suggestions are simple API responses Clear separation: readers suggest, humans decide Negative Readers share process memory A bad reader can affect API performance All readers must be Python Mitigations Timeouts on reader scan operations Input validation before passing to readers Consider process isolation if readers become untrusted Reader Interface References [Python Plugin Patterns](https://packaging.python.org/en/latest/guides/creating and discovering plugins/) [Entry Points for Plugins](https://setuptools.pypa.io/en/latest/userguide/entry point.html)"},{"slug":"architecture/adr/004-multi-label-node-model","title":"ADR-004 · Multi-label node model","href":"/docs/architecture/adr/004-multi-label-node-model","excerpt":"ADR 004: Multi Label Node Model in Neo4j Date: 2026 01 27 Status: Accepted Deciders: Tom Context Holocron tracks different types of entities: Assets: Dataset, Report, Process, System Actors: Person, Group We need to deci","body":"ADR 004: Multi Label Node Model in Neo4j Date: 2026 01 27 Status: Accepted Deciders: Tom Context Holocron tracks different types of entities: Assets: Dataset, Report, Process, System Actors: Person, Group We need to decide how to model these types in Neo4j. Decision Use multiple labels per node: a base label + type specific label. Options Considered 1. Single Label + Type Property Pros: Simple queries: Easy to add new types (no schema change) Cons: Can't use label specific indexes Queries for specific type: (slower) No Neo4j level type enforcement 2. Separate Labels Only Pros: Clean separation Type specific indexes Cons: Hard to query \"all assets\" without UNION No shared base for common operations Relationships must list all types 3. Multiple Labels ✅ Selected Pros: Query all assets: Query specific type: Label specific indexes work Neo4j native pattern Cons: Must remember to add both labels Slightly more complex node creation Rationale This is Neo4j's recommended pattern for type hierarchies. It gives us: 1. Flexible Querying 2. Efficient Indexing 3. Type Safety in Relationships 4. Easy Extension Adding a new type is just a new label combination: Implementation Creating Nodes Constraints & Indexes Consequences Positive Idiomatic Neo4j usage Efficient queries at both levels Easy to extend with new types Clear data model Negative Must ensure both labels are set on creation Queries must use correct label for intent Mitigations Repository layer enforces label assignment Type enums in Python ensure valid types Integration tests verify label correctness References [Neo4j: Labels Best Practices](https://neo4j.com/developer/guide data modeling/) [Neo4j: Multiple Labels](https://neo4j.com/docs/cypher manual/current/clauses/create/ create create a node with multiple labels)"},{"slug":"architecture/adr/005-hybrid-search","title":"ADR-005 · Hybrid search","href":"/docs/architecture/adr/005-hybrid-search","excerpt":"ADR 005: Hybrid vector + fulltext search Date: 2026 04 25 Status: Accepted Deciders: Tom Context Holocron needs catalog wide search that finds the right entity whether the user types: a literal substring of a column name","body":"ADR 005: Hybrid vector + fulltext search Date: 2026 04 25 Status: Accepted Deciders: Tom Context Holocron needs catalog wide search that finds the right entity whether the user types: a literal substring of a column name ( ), a fuzzy concept ( ), a quoted phrase ( ), a graph constraint ( ). Pure keyword search misses semantic intent. Pure vector search misses literal matches and is awful at \"find me the column literally called \". Both give bad results when the catalog has many similarly named entities (asset → schema container → field). We need ranking that combines both signals, doesn't drown parent assets in their own columns, and has a small DSL for power users. Decision Run both rankers per kind and fuse their scores with a probabilistic OR : On top of that: Per kind discounts so schema nodes don't dominate their parents: , . Vector only penalty when fulltext is dominant: if any FTS hit ≥ , all pure vector hits are scaled by . This prevents a soft matching asset from outranking a literal matching field. Per kind cap of 20 hits to keep response sizes bounded. Tiebreak by kind order so deterministic results for equal scores. Embeddings: (384 dim, cosine) via fastembed (ONNX, CPU only, ~300 MB resident, ~1 s warm up). Indexed in Neo4j vector indexes alongside Lucene fulltext indexes on . DSL: bare words = semantic + fulltext; prefixes ( , , ) scope kind; named operators ( , , , , ) resolve to UID sets via fulltext and constrain results; requires literal substring; excludes. Options considered 1. Pure keyword (Lucene only) Pros: simple, fast, no model download, works offline. Cons: misses semantic intent, awful at typos and synonyms, no relevance to query meaning. 2. Pure vector Pros: semantic match, ranks \"concept matches\" well. Cons: terrible at literal lookups ( ), terrible at proper nouns, returns lots of \"kinda close\" garbage when nothing exists. 3. External search engine (Elasticsearch / OpenSearch / Qdrant) Pros: purpose built, more knobs. Cons: extra service to operate, sync to maintain, second source of truth. Not justified for the catalog sizes Holocron targets. 4. Hybrid in Neo4j ✅ Selected Pros: one storage layer, one transaction model, one operational footprint. Both index types are first class in Neo4j 5. Cons: Neo4j's vector indexes are newer than dedicated vector DBs; tuning is up to us. Rationale The catalog is small enough (target: tens of thousands of entities) that there's no operational case for a separate search service. Fusing both signals in one query keeps the architecture simple. The discounts and penalties were tuned empirically against the Star Wars fixture and the dogfood catalog — they're constants and live in so they're easy to revisit. The DSL is intentionally lax (unknown operators silently fall through to plain text). That's a power user comfort vs. error feedback trade off. The expected user is technical and forgiving. Consequences Positive One database, one set of indexes, one query path. Hybrid ranking handles both literal and semantic queries well. Scoring constants are pure data — easy to tune, easy to test ( ). Schema nodes are searchable individually but don't drown parent assets. Negative Embedding model adds memory pressure (~300 MB resident). First search after API start is slow until the model is loaded. Tuning constants is empirical; no automated relevance test. Neo4j vector indexes are newer; behaviour at scale (millions of entities) is less proven than dedicated vector DBs. Mitigations Pre warm the model at boot if needed. Move to a dedicated vector store (Qdrant or similar) only when catalog size or query volume justifies the operational cost. References [Neo4j vector indexes](https://neo4j.com/docs/cypher manual/current/indexes/semantic indexes/vector indexes/) [Neo4j fulltext indexes](https://neo4j.com/docs/cypher manual/current/indexes/search performance indexes/full text indexes/) [BAAI/bge small en v1.5](https://huggingface.co/BAAI/bge small en v1.5) Implementation: Public "},{"slug":"architecture/adr/006-plugin-sdk-entry-points","title":"ADR-006 · Plugin SDK + entry points","href":"/docs/architecture/adr/006-plugin-sdk-entry-points","excerpt":"ADR 006: Plugin SDK + entry point discovery Date: 2026 04 26 Status: Accepted (supersedes [ADR 003](003 reader plugin architecture.md)) Deciders: Tom Context The MVP era \"reader\" system ([ADR 003](003 reader plugin archi","body":"ADR 006: Plugin SDK + entry point discovery Date: 2026 04 26 Status: Accepted (supersedes [ADR 003](003 reader plugin architecture.md)) Deciders: Tom Context The MVP era \"reader\" system ([ADR 003](003 reader plugin architecture.md)) had two assumptions that didn't survive contact with real use: 1. \"Suggest don't write.\" Readers returned objects; humans turned them into entities through a separate API call. Every connector ended up reimplementing the suggest→create plumbing, and the human in the loop step was a friction point for batch imports — most users wanted \"scan and ingest\". The audit trail was already provided by + , so the additional approval step was redundant. 2. Reader only. Real workflows needed exporters (catalog → file), audit plugins (catalog → findings), and detector plugins (catalog → suggestions for review). Naming everything a \"reader\" was misleading. We also needed a way for third parties to author plugins without depending on the whole API package (which pulls in FastAPI, Neo4j, embeddings, ...). Decision Replace readers with a unified plugin system: 1. Public SDK package ( ) with the manifest, input spec, result types, runtime context, and Protocol that every plugin satisfies. The API package depends on the SDK; plugins depend only on the SDK. 2. Entry point discovery. Plugins register via the entry point group in . The API discovers them at startup ( ) — no filesystem scan, no manual registry. 3. In process invocation. parses multipart inputs against the manifest, builds a carrying the API's service layer ( , , , , ), and awaits . 4. Two capability families. returns (JSON); returns (streamed file). Audit and detector plugins fit naturally into one of these. 5. CLI bundled with the SDK. for terminal driven invocation, automation, and CI. 6. Drop \"suggest don't write\". Plugins write directly through services. The audit trail is + , surfaced in the UI as a review queue. Options considered 1. Keep the reader system, add exporters separately Pros: less churn. Cons: parallel plumbing for two APIs; readers and exporters end up duplicating manifest/discovery logic. 2. One generic system, but configured by manifest files (YAML) Pros: language agnostic. Cons: extra layer to maintain, harder to test, no help from the type system. Python entry points are already a solved problem. 3. Out of process plugins (subprocess) Pros: isolation; bad plugin can't crash the API. Cons: IPC overhead, harder to debug, harder to share types. Premature for the current scale; revisit if/when plugins become untrusted. 4. Generic plugin system with entry point discovery + public SDK ✅ Selected Pros: single contract, single discovery mechanism, single result type set; SDK lets third parties build without depending on the API; entry points are the standard Python plugin idiom. Cons: plugins share the API process (a bad plugin can affect API performance); discovery only happens at startup (restart needed to add/remove). Rationale Entry points are how Python plugins work. Reusing the standard mechanism means tooling (introspection, packaging, documentation) all just works. The SDK split is the key piece that enables third party plugin authoring without dragging in FastAPI / Neo4j as dependencies. Dropping \"suggest don't write\" reflects what users actually do: import everything, then sweep through the unverified set in the UI. The original design front loaded the cost of approval before any data was in the catalog; the new design defers it, which matches how data teams actually work. Consequences Positive One mental model, one API surface for connectors, exporters, audit, detection. Third parties can write plugins without depending on the API package. The CLI gives automation parity with the UI. makes plugins as powerful as built in services — no awkward intermediate APIs. Negative Plugins share process memory with the API (a bad plugin can affect API performance). Discovery is at startup; adding/removing a plugin requires an API restart. No"},{"slug":"architecture/adr/007-outbound-webhooks","title":"ADR-007 · Outbound webhooks","href":"/docs/architecture/adr/007-outbound-webhooks","excerpt":"ADR 007: Outbound webhooks (HMAC, fire and forget) Date: 2026 04 26 Status: Accepted Deciders: Tom Context Every mutation in Holocron logs an node. External systems (alerting, analytics, governance pipelines) need to rea","body":"ADR 007: Outbound webhooks (HMAC, fire and forget) Date: 2026 04 26 Status: Accepted Deciders: Tom Context Every mutation in Holocron logs an node. External systems (alerting, analytics, governance pipelines) need to react to those events without polling. We need: A way for receivers to subscribe by event topic. Authenticity (the receiver should be able to verify the request came from Holocron). Reasonable failure handling (don't melt the API if a receiver is down). A test path so receivers can validate their integration without waiting for real events. Decision Implement outbound webhooks as a first class entity with HMAC SHA256 signing and fire and forget dispatch: 1. nodes with , (list of topics or ), (HMAC key), , , , , . 2. CRUD via — create returns the secret once ; subsequent reads never expose it. 3. Fan out at event time. When writes an , it triggers which iterates matching subscribers and dispatches each in an . The mutation request returns immediately. 4. HMAC SHA256 over the raw body , sent in . Topic in , event UID in . . 5. Bounded concurrency. Process wide caps in flight deliveries. 6. Auto disable. After 10 consecutive failures the webhook is disabled. Re enable via , which also resets the counter. 7. No persistent retry queue (v0.1). A failed delivery is not retried beyond the immediate attempt. Receivers handle eventual consistency by reconciling against . 8. Test endpoint. fires a synthetic event with and returns . Options considered 1. Polling Pros: trivial — receivers GET . Cons: chatty, latency floor = poll interval, can't push to slack/pagerduty cleanly. 2. Webhooks with persistent retry queue Pros: robust against transient receiver failures. Cons: introduces a queue (Redis / Postgres / RabbitMQ) — extra service to operate. Premature for the current scale. 3. Push to a message broker (Kafka / NATS) Pros: scales horizontally, fan out is the broker's problem. Cons: receivers must speak the broker protocol, extra infrastructure, very different operational model. 4. Outbound HTTP webhooks with HMAC, fire and forget ✅ Selected Pros: zero extra infrastructure; receivers only need an HTTP endpoint and a shared secret; HMAC gives authenticity without TLS only solutions. Cons: transient failures lose events (mitigated by reconciliation); secrets stored plaintext (mitigated by treating the DB as sensitive). Rationale Webhooks are the lowest friction option for receivers — every alerting / governance / orchestration tool understands a signed POST. Fire and forget keeps the API responsive: we never block a write on a slow receiver. HMAC over the raw body is the standard pattern (Stripe, GitHub, ...), familiar to anyone integrating webhooks before. A persistent retry queue is the obvious next step but adds a service. It can be added later without changing the wire contract — receivers wouldn't notice. For now, \"if you missed an event, replay from \" is the documented path. The 10 failure auto disable threshold protects the API from wasting effort on permanently broken receivers. It's high enough to ride out transient outages and low enough that a typoed URL stops sending within a minute or so of meaningful traffic. Consequences Positive Zero extra infrastructure. Standard HMAC pattern, easy to integrate. Mutations never block on receivers. Bad receivers self disable. Negative Failed deliveries aren't retried (in v0.1). Secrets are stored plaintext in Neo4j (the dispatcher needs them to sign). No delivery ordering guarantees across receivers. The semaphore is per process; multiple workers don't share a global concurrency cap. Mitigations Document as the reconciliation path. Treat the Neo4j database as sensitive (it always was — owners and metadata live there too). If at least once delivery becomes a requirement, add a persistent queue without changing the wire contract. References Public reference: [docs/webhooks.md](../../webhooks.md) Routes: Dispatcher: Repository: Tests:"},{"slug":"architecture/adr/008-schema-projection","title":"ADR-008 · Schema projection","href":"/docs/architecture/adr/008-schema-projection","excerpt":"ADR 008: Schema projection — Container/Field nodes Date: 2026 04 25 Status: Accepted Deciders: Tom Context Connectors discover datasets with schema — a CSV has columns, an Excel sheet has tables and columns, a Postgres t","body":"ADR 008: Schema projection — Container/Field nodes Date: 2026 04 25 Status: Accepted Deciders: Tom Context Connectors discover datasets with schema — a CSV has columns, an Excel sheet has tables and columns, a Postgres table has columns with types, a Power BI report references tables and fields. The schema lives naturally in the asset's field as a nested JSON tree (containers → fields). But putting it only in makes it invisible to: Search. Users want to find \"the field of the dataset\", not just \"the dataset that happens to mention email somewhere\". Governance. Rules apply to specific fields ( , ); we need to attach edges scoped to a . Lineage. Column level lineage between datasets requires column nodes to anchor edges to. Storing schema as opaque JSON gives us none of those. Decision Materialise the JSON schema tree as real Neo4j nodes: Triggered automatically whenever an Asset's is written. Implemented as a full rebuild of that asset's subtree: 1. Tear down: detach delete every and reachable from the asset. 2. Materialise: walk the new schema JSON, creating containers and fields with denormalised , , slash joined (e.g. ). 3. Embed: the embedding service produces a 384 dim vector for each new container and field, populating the same vector + fulltext indexes used by other entity kinds. Container and Field nodes are derived state — they're not directly created or edited via the API. Editing them happens through the parent asset's . Source: . Options considered 1. Keep schema as JSON in Pros: simple, no projection step. Cons: invisible to search and governance. Users have to write Cypher to find a column. 2. Materialise lazily on first read Pros: writes stay cheap. Cons: the first read after a write is slow and unpredictable. Search needs the indexes populated now , not on first query. Hard to keep consistent. 3. Store as a child Neo4j sub API (POST , ...) Pros: explicit lifecycle. Cons: users now have two APIs to keep in sync; connectors have to make N+M calls per asset; the JSON in becomes ambiguous (which is canonical?). 4. Materialise on every write ✅ Selected Pros: the JSON in stays canonical (one source of truth); the graph stays consistent; search and governance work without special casing schema; connectors only need to set . Cons: every asset write rebuilds the subtree even if the schema didn't change. Cost is bounded by per asset schema size. 5. Diff based incremental rebuild Pros: cheaper at scale. Cons: more code, more failure modes (drifting state if the diff is wrong). Premature for current scale. Rationale JSON in is what connectors naturally produce, and it's the easiest authoring path for hand editing. Materialising on write keeps the JSON canonical (it's the input; the nodes are the output) without forcing a second authoring API. The full rebuild is dumb but correct — diff based projection can come later if writes become a bottleneck. The denormalisation of , , and onto every Container/Field means search results can render \"the field of the table\" without a join back to the parent. This trades a little storage for a lot of read simplicity, which is worth it for the catalog scale we target. Consequences Positive Schema is a first class part of the graph: search, governance, lineage all see fields and containers. The JSON in stays canonical — no two source of truth problem. Connectors are unchanged: just set . Search hits gain and for free. Rules can attach to specific fields via on . Negative Every asset write rebuilds the subtree, even if the schema didn't change. At very deep schemas ( 1000 fields per asset) the rebuild + embedding cost is noticeable. Container/Field UIDs are stable per asset+path but not portable (if the asset is renamed, paths change). Mitigations Skip the rebuild if the schema field didn't change in the update payload (cheap diff at the route layer — TODO). Move embedding to a batch job at scale (the same way the graph layout is currently cached at startup). References Public reference: [do"},{"slug":"__catalog__","title":"Plugin catalog","href":"/plugins","excerpt":"Browse the 9 built-in plugins (CSV, Excel, Postgres, Power BI, Excel exporter, Markdown exporter, lineage gap audit, compliance report, PII detector).","body":"plugins catalog connectors exporters audit pii postgres powerbi excel csv markdown compliance lineage"}]